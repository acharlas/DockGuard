[production]
# Connect via Cloudflare Tunnel SSH (zero-trust, no public SSH port exposed)
# Requires cloudflared CLI locally and CF_ACCESS_CLIENT_ID/SECRET env vars.
ssh.dockguard.acharlas.dev ansible_user=ubuntu
